/** Altura (eje Y, tres.js) de la barra del burro en RackScene; compartida con ShirtMesh3D. */
export const RACK_BAR_Y = 100;

/** Separación entre perchas y margen a los lados; compartido con Closet (cálculo del ancho de la escena). */
export const RACK_SPACING = 56;
export const RACK_PADDING = 70;
